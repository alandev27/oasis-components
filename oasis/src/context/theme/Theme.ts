export interface Theme {
    textColor: string;
    textPlaceholderColor: string;
    textAccentColor: string;
    backgroundColor: string;
    borderColor: string;
    backgroundSelectedColor: string;
    accentColor: string;
    accentSelectedColor: string;
    icon: React.ReactNode;
}

export default Theme;