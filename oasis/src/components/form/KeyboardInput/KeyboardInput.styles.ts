import { StyleSheet } from "../../../context/OasisProvider";

export const InputTextAreaStyle: StyleSheet = (theme) => ({
    backgroundColor: theme!.backgroundColor,
    color: theme!.accentColor,
    border: `1px solid ${theme!.borderColor}`,
    borderRadius: '5px',
    padding: '0.6rem',
    outline: 'none',
})

export const InputStyle: StyleSheet = (theme) => ({
    backgroundColor: theme!.backgroundColor,
    color: theme!.accentColor,
    border: `1px solid ${theme!.borderColor}`,
    borderRadius: '5px',
    padding: '0.6rem',
    outline: 'none',
})