import { StyleSheet } from "../../../context/OasisProvider"

export const DropdownStyles: StyleSheet = () => ({
    position: 'relative',
    display: 'inline-block',
});

export const DropdownItemStyles: StyleSheet = () => ({
    position: 'absolute',
    minWidth: '8rem',
});